<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Sign Up - modelmarket.ai</title>
  <link rel="stylesheet" href="/styles/login.css" />
</head>
<body>
  <div class="login-container">
    <h2 class="login-heading">Sign Up Here!</h2>
    <form class="login-box" action="/auth/signup" method="POST">
      <input type="text" 
             name="fullName"
             placeholder="Full Name"
             class="input" required />
      <div class="date-container">
        <input type="date" 
               name="dateOfBirth"
               class="input"
               required />
        <span class="date-placeholder">Date of Birth</span>
      </div>
      <input type="email" 
             name="email"
             placeholder="Email" 
             class="input"
             title="Email must be valid and end with gmail.com, yahoo.com, yahoo.in, or outlook.com"
             required />
      <input type="password" 
             name="password"
             placeholder="Password" 
             class="input"
             title="Password must be at least 8 characters, one number, and one special character (@, #, _)"
             required />
      <button type="submit" class="login-btn">Sign Up</button>
      <p class="register-text">
        <a href="/auth/login">Already have an account? Log In</a>
      </p>
    </form>
  </div>
</body>
</html>